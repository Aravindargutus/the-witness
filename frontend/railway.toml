# Railway config for the FRONTEND service
# Set this service's Root Directory to: the-witness/frontend/

[build]
  builder = "DOCKERFILE"
  dockerfilePath = "Dockerfile"

[deploy]
  healthcheckPath = "/"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
